"""Standalone application widgets for pyvistra.

These are self-contained tools that build on the core library but are not
part of the core API.  Import them explicitly when needed::

    from pyvistra.apps.console import EmbeddedConsole

Gel electrophoresis analysis lives in the separate ``gelkit`` package (a
pyvistra plugin, see ``pyvistra.plugins``), not in core.
"""
